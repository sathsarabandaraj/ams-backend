// src/enums/user.enum.ts
export enum Gender {
    MALE = 'male',
    FEMALE = 'female',
    OTHER = 'other'
}

export enum CivilStatus {
    SINGLE = 'single',
    MARRIED = 'married',
    DIVORCED = 'divorced',
    WIDOWED = 'widowed'
}

export enum PreferredMode {
    ONLINE = 'online',
    ONSITE = 'onsite'
}
